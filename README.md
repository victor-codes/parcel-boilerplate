## To start the boilerplate, run the following commands:
```
yarn
```
```
yarn dev
```
## If you don't have `yarn` installed, delete the `yarn.lock` file and install via npm,
```
npm install
```
```
npm run dev
```
